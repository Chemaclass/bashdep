{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "includeCoAuthoredBy": false,
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=\"$CLAUDE_FILE_PATH\"; if [ -n \"$file\" ] && { echo \"$file\" | grep -qE '\\.sh$|/bashdep$|/pre-commit$'; }; then shellcheck -e SC1091 -e SC2155 -x \"$file\" 2>&1 | head -20; fi"
          }
        ]
      }
    ]
  }
}
